<% @notices.each do |n| %>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title"><%= n.title %> <small>Publicado por: <%= n.user.first_name %></small></h3>
  </div>
  <div class="panel-body">
    <%= n.description %>
    <a href="<%= notice_path(n)%>" class="btn btn-info pull-right">Ver más</a>
  </div>
</div>
<p></p>
<% end %>
