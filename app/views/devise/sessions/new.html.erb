<div class="row text-center" style="padding:5%">

  <div class="col-md-8 well">
  <h1>Bienvenido a SaveYourCity</h1>
  <p><strong>SaveYourCity</strong> es una plataforma de colaboracion ciudadana para avisar de incidentes, noticias o cualquier evento que ocurra en tu zona. Podras subir fotos, y los demas sabremos donde esta gracias a la geolocalizacion.</p>
  </div>
  <div class="col-md-3 col-md-offset-1 well well-sm">

  <h2>Acceder al sitio</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Tu email' %>
    </div>

    <div class="field">
      <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Contraseña ' %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :recordarme %>
      </div>
    <% end -%>

    <div class="actions">
      <%= f.submit "Conectarse", class: 'btn btn-default btn-block' %>
    </div>
  <% end %>
  <hr>
  <%= link_to user_omniauth_authorize_path(:facebook) do %>
		<button class="btn btn-lg btn-social btn-facebook">
    	<i class="fa fa-facebook"></i> Acceder con Facebook
    </button>
  <% end %>
  <hr>

  <%= render "devise/shared/links" %>
  </div>
</div>
